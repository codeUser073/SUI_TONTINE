/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed turbopack config as it's not supported in Next.js 14
  // SVG handling is done through other means
};

export default nextConfig;